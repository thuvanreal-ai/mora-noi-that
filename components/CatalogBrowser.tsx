"use client";
import {useState} from 'react';import {catalog} from '@/lib/catalog';import ProductCard from './ProductCard';
export default function CatalogBrowser(){const [category,setCategory]=useState('Tất cả');const [query,setQuery]=useState('');const categories=['Tất cả',...new Set(catalog.map(p=>p.category))];const normalize=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').toLowerCase();const visible=catalog.filter(p=>(category==='Tất cả'||p.category===category)&&normalize(p.name).includes(normalize(query)));return <><div className="catalogTools"><label>Tìm sản phẩm<input className="input" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tủ, giường, bàn…"/></label><label>Nhóm sản phẩm<select className="input" value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><p className="muted" aria-live="polite">{visible.length} mẫu phù hợp</p><div className="productGrid">{visible.map(p=><ProductCard key={p.slug} product={p}/>)}</div>{!visible.length&&<p>Chưa tìm thấy mẫu phù hợp. Thử từ khóa khác hoặc <a href="../nhan-bao-gia/">gửi nhu cầu riêng</a>.</p>}</>}
