import Link from 'next/link';import Header from './Header';import Footer from './Footer';import ProductConfigurator from './ProductConfigurator';import ProductImage from './ProductImage';import ProductCard from './ProductCard';import {catalog,type CatalogProduct} from '@/lib/catalog';import {site} from '@/lib/site';
export default function ProductDetail({product:p}:{product:CatalogProduct}){
const url=site.url+'/san-pham/'+p.slug+'/';
const schema={'@context':'https://schema.org','@graph':[{'@type':'Product',name:p.name,description:p.description+' Giá dự toán, cần xác nhận cấu hình.',url,brand:{'@type':'Brand',name:'MORA'},image:site.url+'/images/'+p.image+'-960.webp',material:'MDF phủ Melamine',hasVariant:p.variants.map(v=>({'@type':'Product',name:p.name+' '+v.label,sku:v.sku,description:'Cấu hình dự toán; chưa phải báo giá đã được xưởng xác nhận.'}))},{'@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Trang chủ',item:site.url+'/'},{'@type':'ListItem',position:2,name:'Sản phẩm',item:site.url+'/san-pham/'},{'@type':'ListItem',position:3,name:p.name,item:url}]}]};
return <><Header/><main className="section"><div className="container"><nav className="breadcrumb" aria-label="Đường dẫn"><Link href="/">Trang chủ</Link><span>/</span><Link href="/san-pham">Sản phẩm</Link><span>/</span><span>{p.name}</span></nav><div className="productDetail"><div className="productGallery"><ProductImage asset={p.image} alt={'Phối cảnh '+p.name}/><p className="muted">Ảnh minh họa thiết kế, không phải công trình thực tế. Số cánh, bố cục và màu sắc được xác nhận theo cấu hình đặt hàng.</p></div><div><p className="eyebrow">{p.category}</p><h1>{p.name}</h1><p>{p.description}</p><p className="muted">{p.material}</p><ProductConfigurator product={p}/></div></div><section className="section"><h2>Có thể bạn cũng cần</h2><div className="productGrid">{catalog.filter(x=>x.slug!==p.slug).slice(0,3).map(x=><ProductCard key={x.slug} product={x}/>)}</div></section></div></main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\\u003c')}}/><Footer/></>}
