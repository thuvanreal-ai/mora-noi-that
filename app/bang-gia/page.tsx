import Header from "@/components/Header";import Footer from "@/components/Footer";import Link from "next/link";import {catalog} from "@/lib/catalog";import {money} from "@/lib/pricing";export const metadata={title:"Bảng giá tham khảo nội thất MDF",description:"Giá tham khảo theo kích thước sản phẩm MORA; báo giá thực tế xác nhận theo vật liệu, phụ kiện và kích thước."};export default function Page(){return <><Header/><main className="section"><div className="container"><h1>Bảng giá tham khảo nội thất MDF</h1><p className="muted">Các mức dưới đây là mô hình giá để tham khảo, chưa phải báo giá xưởng chính thức. Báo giá cuối cùng được xác nhận theo kích thước, vật liệu, phụ kiện, vận chuyển và lắp đặt thực tế.</p><div className="grid4">{catalog.map(p=><div className="card" key={p.slug}><div className="cardBody"><h3>{p.name}</h3>{p.variants.map(v=><p key={v.sku}><b>{v.label}</b><br/>{money(v.list)}</p>)}<Link href={"/san-pham/"+p.slug}>Xem cấu hình →</Link></div></div>)}</div><div className="actions"><Link className="btn primary" href="/nhan-bao-gia">Nhận báo giá theo kích thước</Link><a className="btn" href="tel:0916858566">Gọi 0916 85 85 66</a></div></div></main><Footer/></>}