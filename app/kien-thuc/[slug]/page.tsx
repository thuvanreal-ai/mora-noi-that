import {notFound} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import {cms} from "@/lib/cms";export function generateStaticParams(){return cms.posts().map(x=>({slug:x.slug}))}export function generateMetadata({params}:{params:{slug:string}}){const p=cms.posts().find(x=>x.slug===params.slug);return {title:p?.title||"Kiến thức nội thất"}}export default function Page({params}:{params:{slug:string}}){const p=cms.posts().find(x=>x.slug===params.slug);if(!p)return notFound();return <><Header/><article className="section"><div className="container" style={{maxWidth:850}}><p className="muted">Kiến thức / Nội thất MDF</p><h1>{p.title}</h1><p>{p.excerpt}</p><p className="muted">Nội dung chuyên môn đầy đủ sẽ được cập nhật sau khi đối chiếu dữ liệu vật liệu và tiêu chuẩn của xưởng.</p></div></article><Footer/></>}