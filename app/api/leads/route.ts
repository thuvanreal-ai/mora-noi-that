import {NextResponse} from "next/server";export async function POST(req:Request){const form=await req.formData();const lead={name:form.get("name"),phone:form.get("phone"),product:form.get("product"),size:form.get("size"),address:form.get("address"),note:form.get("note"),createdAt:new Date().toISOString()};console.log("MORA_LEAD",lead);return NextResponse.redirect(new URL("/cam-on",req.url),303);}