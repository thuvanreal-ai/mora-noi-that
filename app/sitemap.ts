export const dynamic='force-static';import type {MetadataRoute} from 'next';import {catalog} from '@/lib/catalog';import {site} from '@/lib/site';
export default function sitemap():MetadataRoute.Sitemap{const pages=['','/san-pham','/theo-khong-gian','/noi-that-theo-yeu-cau','/cong-trinh','/kien-thuc','/bang-gia','/ve-xuong','/nhan-bao-gia','/lien-he'];return [...pages.map(p=>({url:site.url+p+'/'})),...catalog.map(p=>({url:site.url+'/san-pham/'+p.slug+'/'}))]}
