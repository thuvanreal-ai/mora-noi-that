import Header from "@/components/Header";import Footer from "@/components/Footer";import Link from "next/link";import {catalog} from "@/lib/catalog";import {money} from "@/lib/pricing";export default function Page(){return <><Header/><main className="section"><div className="container"><h1>Sản phẩm MORA</h1><p className="muted">Catalog được thiết kế theo kích thước chuẩn và mô hình giá thành. Chọn sản phẩm để đổi kích thước và xem giá bán.</p><div className="grid4">{catalog.map(p=>{const v=p.variants[0];return <Link className="card" href={"/san-pham/"+p.slug} key={p.slug}><div className="thumb"/><div className="cardBody"><b>{p.name}</b><div><span className="discount">Giá tham khảo</span></div><div className="price">{money(v.list)}</div><small>{v.label}</small></div></Link>})}</div></div></main><Footer/></>}