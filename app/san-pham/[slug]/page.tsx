import {notFound} from 'next/navigation';import ProductDetail from '@/components/ProductDetail';import {catalog} from '@/lib/catalog';import {site} from '@/lib/site';
export function generateStaticParams(){return catalog.filter(p=>p.slug!=='tu-quan-ao-mdf-modern').map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=catalog.find(x=>x.slug===slug);return{title:p?.name||'Sản phẩm MORA',description:p?.description,alternates:{canonical:site.url+'/san-pham/'+slug+'/'},openGraph:{images:p?[{url:site.url+'/images/'+p.image+'-960.webp',alt:'Phối cảnh '+p.name}]:[]}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=catalog.find(x=>x.slug===slug);if(!p)return notFound();return <ProductDetail product={p}/>}
