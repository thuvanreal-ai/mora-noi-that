import {notFound} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import ProductConfigurator from "@/components/ProductConfigurator";import {catalog} from "@/lib/catalog";export function generateStaticParams(){return catalog.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=catalog.find(x=>x.slug===slug);return{title:p?.name||"Sản phẩm MORA",description:p?.description}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=catalog.find(x=>x.slug===slug);if(!p)return notFound();return <><Header/><main className="section"><div className="container"><p className="muted">Sản phẩm / {p.category}</p><div className="custom productHero"><div className="productPlaceholder"><span>MORA</span><b>{p.name}</b></div><div><h1>{p.name}</h1><p>{p.description}</p><p className="muted">Giá hiện là mô hình nghiên cứu; sẽ hiệu chỉnh theo BOM và giá mua thực tế của xưởng trước khi chạy quảng cáo.</p><ProductConfigurator product={p}/></div></div></div></main><Footer/></>}