import {notFound} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import {products} from "@/lib/data";import Link from "next/link";export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default function Page({params}:{params:{slug:string}}){const p=products.find(x=>x.slug===params.slug);if(!p)return notFound();return <><Header/><main className="section"><div className="container"><p className="muted">Trang chủ / Sản phẩm / {p.name}</p><div className="custom" style={{background:"#fff",color:"#252522"}}><div><div className="thumb" style={{height:420,borderRadius:16}}/></div><div><h1>{p.name}</h1><h2>{p.price}</h2><p>Kích thước: {p.size}</p><p>Vật liệu, phụ kiện, thời gian sản xuất và chính sách bảo hành đang chờ bảng dữ liệu chính thức từ xưởng.</p><div className="actions"><Link className="btn primary" href="/nhan-bao-gia">Nhận báo giá đúng kích thước</Link><a className="btn" href="tel:0916858566">Gọi 0916 85 85 66</a></div></div></div></div></main><Footer/></>}