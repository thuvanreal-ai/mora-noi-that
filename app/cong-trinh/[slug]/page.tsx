import {notFound} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import {cms} from "@/lib/cms";export function generateStaticParams(){return cms.projects().map(x=>({slug:x.slug}))}export default function Page({params}:{params:{slug:string}}){const p=cms.projects().find(x=>x.slug===params.slug);if(!p)return notFound();return <><Header/><main className="section"><div className="container"><p className="muted">Công trình / MORA</p><h1>{p.name}</h1><div className="thumb" style={{height:420,borderRadius:18}}/><p>{p.summary}</p><p className="muted">Chưa sử dụng hình ảnh hoặc giá trị công trình giả.</p></div></main><Footer/></>}