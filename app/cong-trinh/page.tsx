import Header from "@/components/Header";import Footer from "@/components/Footer";import {projects} from "@/lib/data";export const metadata={title:"Công trình nội thất MDF | MORA"};export default function Page(){return <><Header/><main className="section"><div className="container"><h1>Công trình thực tế</h1><p className="muted">Khu vực này sẽ cập nhật case thực tế sau khi có ảnh và dữ liệu từ xưởng.</p><div className="grid4">{projects.map(p=><div className="card" key={p.slug}><div className="thumb"/><div className="cardBody"><b>{p.name}</b><p>{p.summary}</p></div></div>)}</div></div></main><Footer/></>}